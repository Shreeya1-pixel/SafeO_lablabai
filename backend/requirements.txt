# For AMD ROCm: install torch via amd_setup/install_rocm.sh before pip install -r requirements.txt
#
# Optional GPU / local LLM stack (install via install_rocm.sh or pin separately):
#   torch (ROCm wheel from https://download.pytorch.org/whl/rocm6.2)
#   transformers, vllm, accelerate, sentencepiece, numpy, scipy

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6
requests==2.32.3
python-multipart==0.0.20
httpx==0.28.1
python-dotenv>=1.0.0
openai>=1.40.0
band-sdk>=0.1.0

# ML / AMD (CPU-only hosts can omit torch/vllm; heuristic engine still runs)
transformers>=4.40.0
accelerate>=0.30.0
sentencepiece>=0.2.0
numpy>=1.26.0
scipy>=1.11.0
